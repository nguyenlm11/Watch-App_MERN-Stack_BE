<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Change Password</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@1.0.0/css/bulma.min.css">
</head>

<body>
    <%- include('../components/navbar') %>

        <div class="container">
            <h1 class="title">Change Password</h1>
            <p class="subtitle">Enter your current and new password below.</p>

            <form action="/auth/change-password" method="POST">
                <div class="field">
                    <label class="label">Current Password</label>
                    <div class="control">
                        <input class="input" type="password" name="currentPassword" placeholder="Current Password"
                            required>
                    </div>
                </div>

                <div class="field">
                    <label class="label">New Password</label>
                    <div class="control">
                        <input class="input" type="password" name="newPassword" placeholder="New Password" required>
                    </div>
                </div>

                <div class="field">
                    <label class="label">Confirm New Password</label>
                    <div class="control">
                        <input class="input" type="password" name="confirmNewPassword"
                            placeholder="Confirm New Password" required>
                    </div>
                </div>

                <div class="field">
                    <div class="buttons" style="display: flex; justify-content: flex-end">
                        <button class="button is-primary" type="submit">Change Password</button>
                        <a href="/auth/profile" class="button is-danger">Cancel</a>
                    </div>
                </div>
            </form>

            <% if (typeof error !=='undefined' ) { %>
                <div class="notification is-danger" style="margin-top: 1rem;">
                    <%= error %>
                </div>
                <% } %>
        </div>
</body>

</html>